<?xml version="1.0" encoding="utf-8"?>
<com.google.android.material.card.MaterialCardView
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:layout_marginHorizontal="@dimen/spacing_md"
    android:layout_marginTop="@dimen/spacing_md"
    app:cardCornerRadius="@dimen/card_corner_radius"
    app:cardElevation="@dimen/card_elevation"
    app:cardBackgroundColor="?attr/colorSurface">

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="vertical"
        android:padding="@dimen/spacing_lg">

        <!-- Brand + Material Name -->
        <TextView
            android:id="@+id/tvBrandName"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            style="@style/TextAppearance.OpenPrintTag.HeadlineMedium"
            android:textColor="?attr/colorOnSurface"
            android:text="Polymaker PolyLite PETG" />

        <!-- Hidden - we combine into brandName -->
        <TextView
            android:id="@+id/tvMaterialName"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:visibility="gone" />

        <!-- Material Class + Type -->
        <TextView
            android:id="@+id/tvMaterialInfo"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:layout_marginTop="@dimen/spacing_xs"
            style="@style/TextAppearance.OpenPrintTag.BodyMedium"
            android:textColor="?attr/colorOnSurfaceVariant"
            android:text="FFF • PETG" />

        <!-- Color Swatches -->
        <LinearLayout
            android:id="@+id/colorContainer"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:layout_marginTop="@dimen/spacing_md"
            android:orientation="horizontal"
            android:gravity="start|center_vertical" />

    </LinearLayout>

</com.google.android.material.card.MaterialCardView>
